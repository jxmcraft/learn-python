# How to set up
```
python -m venv scapy_env
source scapy_env/bin/activate
pip install scapy
```
# What I learnt
1. Setting up an environment
2. How to use scapy library (sniff(prn = function), packet.haslayer(Raw), packet[Raw])
3. Make cleaner code by refactoring into a class
4. Some of the basics for numpy again (bincount(), frombuffer())
5. Relearn how to make a csv file (open(filepath, 'w') as csv_file -> csv.writer(csv_file))
6. Some knowledge for TCP, networking